/** Nomes das coleções do Firestore — única fonte da verdade para evitar strings soltas. */
export const COLLECTIONS = {
  USERS: 'users',
  ACCOUNTS: 'accounts',
  TRANSFERS: 'transfers',
} as const;
